# TAMS Backend — runtime dependencies
# Trimmed to what the app actually imports so it builds on Railway/Render.
# (Heavy ML/geo libs like torch, tensorflow, gdal, rasterio are not used at
#  runtime and lived in requirements-full.txt for future local ML work.)

fastapi==0.104.1
uvicorn[standard]==0.24.0
starlette==0.27.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
python-multipart==0.0.6
python-json-logger==2.0.7
httpx==0.25.2
sqlalchemy[asyncio]==2.0.23
asyncpg==0.29.0
geoalchemy2==0.14.2
greenlet==3.0.1
